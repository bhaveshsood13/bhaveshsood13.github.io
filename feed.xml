<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://bhaveshsood13.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://bhaveshsood13.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-12T06:54:53+00:00</updated><id>https://bhaveshsood13.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Understanding the Transformers Library</title><link href="https://bhaveshsood13.github.io/blog/2026/transformers-library/" rel="alternate" type="text/html" title="Understanding the Transformers Library"/><published>2026-03-14T14:00:00+00:00</published><updated>2026-03-14T14:00:00+00:00</updated><id>https://bhaveshsood13.github.io/blog/2026/transformers-library</id><content type="html" xml:base="https://bhaveshsood13.github.io/blog/2026/transformers-library/"><![CDATA[<p>The <code class="language-plaintext highlighter-rouge">transformers</code> library is the part of Hugging Face most people touch first.</p> <p>At a high level, it is a library of transformer-based models with a consistent interface for loading checkpoints, preprocessing inputs, running inference, and fine-tuning models.</p> <p>That sounds simple, but the reason it is powerful is the structure underneath.</p> <h2 id="the-three-core-building-blocks">The three core building blocks</h2> <p>For each model family, the library is organized around three main classes:</p> <ol> <li><code class="language-plaintext highlighter-rouge">Model</code> classes</li> <li><code class="language-plaintext highlighter-rouge">Configuration</code> classes</li> <li><code class="language-plaintext highlighter-rouge">Preprocessing</code> classes</li> </ol> <h3 id="model-classes">Model classes</h3> <p>These define the actual neural network architecture. Depending on the framework, that may be a PyTorch model, a TensorFlow <code class="language-plaintext highlighter-rouge">tf.keras.Model</code>, or a Flax/JAX model.</p> <p>You will often see two broad variants:</p> <ul> <li>a base model like <code class="language-plaintext highlighter-rouge">AutoModel</code> or <code class="language-plaintext highlighter-rouge">LlamaModel</code></li> <li>a task-specific model like <code class="language-plaintext highlighter-rouge">AutoModelForCausalLM</code> or <code class="language-plaintext highlighter-rouge">LlamaForCausalLM</code></li> </ul> <p>The difference is important. A base model gives you hidden states. A task-specific model attaches a head for a concrete objective such as causal language modeling.</p> <h3 id="configuration-classes">Configuration classes</h3> <p>Configuration objects store the hyperparameters required to construct the model architecture. That includes things like hidden size, number of layers, vocabulary size, and activation choices.</p> <p>If you are loading an existing checkpoint, you usually do not instantiate the config manually because the library handles it for you.</p> <h3 id="preprocessing-classes">Preprocessing classes</h3> <p>Raw data cannot go directly into the model. Preprocessing classes handle the conversion:</p> <ul> <li>tokenizers for text</li> <li>image processors for vision inputs</li> <li>feature extractors or processors for other modalities</li> </ul> <p>They make sure input data is shaped in the exact format the model expects.</p> <h2 id="two-apis-that-make-the-library-approachable">Two APIs that make the library approachable</h2> <p>On top of those classes, the library exposes two especially important APIs:</p> <ul> <li><code class="language-plaintext highlighter-rouge">pipeline()</code> for quick inference</li> <li><code class="language-plaintext highlighter-rouge">Trainer</code> for fast PyTorch training or fine-tuning</li> </ul> <p><code class="language-plaintext highlighter-rouge">pipeline()</code> is useful when you want the shortest path from model to result. It hides most of the wiring and is great for demos or quick experiments.</p> <p><code class="language-plaintext highlighter-rouge">Trainer</code> is useful when you want a structured training loop without rebuilding the whole training stack yourself.</p> <h2 id="the-methods-that-show-up-everywhere">The methods that show up everywhere</h2> <p>Three methods define much of the standard workflow:</p> <ul> <li><code class="language-plaintext highlighter-rouge">from_pretrained()</code></li> <li><code class="language-plaintext highlighter-rouge">save_pretrained()</code></li> <li><code class="language-plaintext highlighter-rouge">push_to_hub()</code></li> </ul> <h3 id="from_pretrained"><code class="language-plaintext highlighter-rouge">from_pretrained()</code></h3> <p>This method loads a pretrained configuration, model, and often preprocessing artifacts from either:</p> <ul> <li>a model ID on the Hub</li> <li>a local directory on disk</li> </ul> <p>This is the method that turns an architecture into something useful by loading checkpoint weights.</p> <p>For example:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoModelForCausalLM</span>

<span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">meta-llama/Llama-2-7b-hf</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">torch_dtype</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">device_map</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <p>The key idea is:</p> <ul> <li>the class tells the library what architecture you want</li> <li><code class="language-plaintext highlighter-rouge">from_pretrained()</code> tells it which trained weights to load</li> </ul> <h3 id="save_pretrained"><code class="language-plaintext highlighter-rouge">save_pretrained()</code></h3> <p>This saves the model, config, and preprocessing artifacts locally so you can reload them later in the same standard format.</p> <h3 id="push_to_hub"><code class="language-plaintext highlighter-rouge">push_to_hub()</code></h3> <p>This publishes those artifacts back to the Hub so they can be shared, versioned, and reused.</p> <h2 id="architecture-versus-checkpoint">Architecture versus checkpoint</h2> <p>One of the most useful distinctions for beginners is:</p> <ul> <li>architecture = the model design</li> <li>checkpoint = the trained weights for that design</li> </ul> <p>For example, BERT is an architecture. A specific repo such as <code class="language-plaintext highlighter-rouge">google-bert/bert-base-uncased</code> is a checkpoint.</p> <p>That is why the same architecture class can load different model IDs.</p> <h2 id="auto-classes-versus-model-specific-classes">Auto classes versus model-specific classes</h2> <p>You can often load models in two styles:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoModelForCausalLM</span><span class="p">,</span> <span class="n">MistralForCausalLM</span>

<span class="n">model_a</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">mistralai/Mistral-7B-v0.1</span><span class="sh">"</span><span class="p">)</span>
<span class="n">model_b</span> <span class="o">=</span> <span class="n">MistralForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">mistralai/Mistral-7B-v0.1</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>The auto class is more generic and convenient. The model-specific class is more explicit when you already know exactly which architecture you are working with.</p> <h2 id="caching-behavior">Caching behavior</h2> <p>Whenever you load a pretrained model, the library downloads files from the Hub and stores them in a local cache.</p> <p>By default, this cache lives under the Hugging Face cache directory, and later loads reuse those files if the cached version is still current.</p> <p>This matters because it affects:</p> <ul> <li>disk usage</li> <li>reproducibility</li> <li>loading speed</li> <li>offline workflows</li> </ul> <p>You can change the cache location with environment variables such as:</p> <ul> <li><code class="language-plaintext highlighter-rouge">HF_HUB_CACHE</code></li> <li><code class="language-plaintext highlighter-rouge">TRANSFORMERS_CACHE</code></li> <li><code class="language-plaintext highlighter-rouge">HF_HOME</code></li> <li><code class="language-plaintext highlighter-rouge">XDG_CACHE_HOME</code></li> </ul> <h2 id="offline-mode">Offline mode</h2> <p>If you are in a firewalled or offline environment, you need the files cached ahead of time. One approach is to download the repository snapshot first and then load locally.</p> <p>You can also:</p> <ul> <li>set <code class="language-plaintext highlighter-rouge">HF_HUB_OFFLINE=1</code> to prevent HTTP calls</li> <li>use <code class="language-plaintext highlighter-rouge">local_files_only=True</code> in <code class="language-plaintext highlighter-rouge">from_pretrained()</code></li> </ul> <p>That makes the library usable in controlled environments where internet access is limited or disallowed.</p> <h2 id="why-the-library-feels-so-productive">Why the library feels so productive</h2> <p>The biggest win is standardization. Once you understand the pattern of:</p> <ul> <li>model class</li> <li>preprocessing class</li> <li><code class="language-plaintext highlighter-rouge">from_pretrained()</code></li> </ul> <p>you can move across many architectures without relearning a completely new API every time.</p> <p>That consistency is what makes <code class="language-plaintext highlighter-rouge">transformers</code> feel much larger than a single library. It is really a common interface over a huge collection of modern model families.</p> <h2 id="closing-thought">Closing thought</h2> <p>If you only remember one thing, remember this:</p> <p><code class="language-plaintext highlighter-rouge">transformers</code> is not just a repository of models. It is a reusable interface for working with architectures, checkpoints, preprocessing, and training workflows in a consistent way.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformers"/><category term="hugging-face"/><category term="llms"/><summary type="html"><![CDATA[A practical guide to model classes, configurations, preprocessing, caching, and loading pretrained checkpoints.]]></summary></entry><entry><title type="html">What Hugging Face Actually Is</title><link href="https://bhaveshsood13.github.io/blog/2026/hugging-face-overview/" rel="alternate" type="text/html" title="What Hugging Face Actually Is"/><published>2026-03-14T13:00:00+00:00</published><updated>2026-03-14T13:00:00+00:00</updated><id>https://bhaveshsood13.github.io/blog/2026/hugging-face-overview</id><content type="html" xml:base="https://bhaveshsood13.github.io/blog/2026/hugging-face-overview/"><![CDATA[<p>When people say “Hugging Face,” they often mean two different things at once:</p> <ul> <li>the company and ecosystem</li> <li>the Hub where models, datasets, and apps are hosted</li> </ul> <p>That distinction matters because the ecosystem is much bigger than the website alone.</p> <h2 id="hugging-face-as-an-ecosystem">Hugging Face as an ecosystem</h2> <p>Hugging Face is an open-source machine learning platform built around making modern models easier to use, share, and deploy. It started with a strong NLP identity, but it now spans vision, audio, and multimodal work too.</p> <p>The ecosystem includes:</p> <ul> <li>libraries like <code class="language-plaintext highlighter-rouge">transformers</code>, <code class="language-plaintext highlighter-rouge">diffusers</code>, <code class="language-plaintext highlighter-rouge">datasets</code>, and <code class="language-plaintext highlighter-rouge">accelerate</code></li> <li>the Hugging Face Hub for hosting models, datasets, and Spaces</li> <li>APIs and tooling that let developers load, fine-tune, evaluate, and share models quickly</li> </ul> <p>In practice, it plays multiple roles at once. It is partly a tooling company, partly a distribution platform, and partly a collaboration layer for modern ML work.</p> <h2 id="what-the-hugging-face-hub-is">What the Hugging Face Hub is</h2> <p>The Hub is the hosted platform at <a href="https://huggingface.co">huggingface.co</a>. This is where model checkpoints, datasets, demo apps, and documentation all become discoverable.</p> <p>If the broader Hugging Face ecosystem is the full operating environment, the Hub is the distribution and sharing surface.</p> <p>That means the Hub is where you:</p> <ul> <li>browse pretrained models</li> <li>upload your own checkpoints</li> <li>version datasets</li> <li>publish demos with Spaces</li> <li>make work reusable for other researchers and engineers</li> </ul> <h2 id="a-simple-way-to-think-about-the-difference">A simple way to think about the difference</h2> <p>The easiest mental model is:</p> <ul> <li>Hugging Face = the full ML ecosystem</li> <li>Hugging Face Hub = the hosted subset used to store, discover, and share assets</li> </ul> <p>The Hub is not the whole story because it does not by itself explain the Python libraries, local training workflows, or inference patterns that developers rely on day to day.</p> <h2 id="why-this-matters-in-practice">Why this matters in practice</h2> <p>A lot of confusion comes from learning the platform through copy-pasted code. You might load a model from the Hub using the <code class="language-plaintext highlighter-rouge">transformers</code> library and assume both are the same thing. They are connected, but they solve different parts of the workflow.</p> <p>For example:</p> <ul> <li><code class="language-plaintext highlighter-rouge">transformers</code> gives you model classes, tokenizers, processors, pipelines, and training utilities</li> <li>the Hub gives you the checkpoint repository those classes pull from</li> </ul> <p>One is the code interface. The other is the distribution layer.</p> <h2 id="why-hugging-face-became-so-useful">Why Hugging Face became so useful</h2> <p>The real value is not just that models are hosted in one place. The value is that the ecosystem reduced friction at every stage:</p> <ul> <li>finding a model</li> <li>loading it with a standard API</li> <li>swapping checkpoints without rewriting everything</li> <li>sharing work back to the community</li> </ul> <p>That combination is why Hugging Face became such a default choice for modern ML experimentation and production prototyping.</p> <h2 id="closing-thought">Closing thought</h2> <p>If you are new to this space, it helps to separate the platform from the libraries immediately. Once that clicks, the rest of the ecosystem makes much more sense.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="hugging-face"/><category term="ml"/><category term="llms"/><summary type="html"><![CDATA[A practical overview of Hugging Face, the Hub, and how the ecosystem fits together.]]></summary></entry></feed>